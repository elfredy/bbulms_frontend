# bbulms_frontend

BBU LMS Next.js frontend.

## Setup

```bash
npm ci
cp .env.example .env.local
npm run dev
```

Production:

```bash
npm run build
npm run start
```

App: `http://localhost:3000` (redirects to `/az`)
